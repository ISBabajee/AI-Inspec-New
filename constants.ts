
export const GEMINI_MODEL_TEXT_IMAGE_INPUT = 'gemini-2.5-flash';

// NOTE: Ensure the API_KEY environment variable is configured in your deployment environment.
// For local development, you might use a .env file with a setup like Vite or Create React App,
// or set it directly in your shell. This application code assumes `process.env.API_KEY` is accessible.

export const APP_TITLE = "AI-Inspec";